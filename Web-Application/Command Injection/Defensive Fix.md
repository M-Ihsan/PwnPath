### Defensive Fix
✅ Never pass user input to system commands

✅ Use language built-in functions instead

✅ Strict input validation — allowlist only

✅ Run web app with minimal OS privileges

❌ Never use exec() or system() with user input

